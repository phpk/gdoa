export enum EventType {
  Click,    // 同时是鼠标按下
  DblClick,
  WebSocket,
  Mqtt,
  MoveIn,
  MoveOut,
  MouseUp
}

export enum EventAction {
  Link,
  StartAnimate,
  Function,
  WindowFn,
  SetProps,
  PauseAnimate,
  StopAnimate,
  Emit
}
